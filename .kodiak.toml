version = 1

[approve]
auto_approve_usernames = ["dependabot"]

[merge.automerge_dependencies]
# auto merge all PRs opened by "dependabot" that are "major", "minor" or "patch" version upgrades
usernames = ["dependabot"]
versions = ["major", "minor", "patch"]
