name: Auto Generate Types

on:
  schedule:
    - cron: '30 * * * *'
  push:
    branches: [master]
  repository_dispatch:
    types: [auto-generate-types]
  workflow_dispatch: # for manual testing

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      # setup
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
      - name: Install dependencies
        run: npm ci
      - name: Get latest types
        run: npm run get-latest-types

      # generate and lint types
      - name: Generate types
        run: npm start -- --new-revisions-only --cache-discovery-json ${{ runner.temp }}
      - name: Lint types
        id: lint
        run: npm run lint
        env:
          GAPI_MAX_PARALLEL: 3

      # upload failed type for investigation
      - name: Copy discovery service response if failed to generate
        if: "${{ github.job.outputs.FAILED_TO_GENERATE_TYPE != '' }}"
        run: cp ${{ runner.temp }}/${{ github.job.outputs.FAILED_TO_GENERATE_TYPE }}.json types/${{ github.job.outputs.FAILED_TO_GENERATE_TYPE }}
      - name: Copy discovery service response if failed to ling
        if: "${{ github.job.outputs.FAILED_TO_LINT_TYPE != '' }}"
        run: cp ${{ runner.temp }}/${{ github.job.outputs.FAILED_TO_LINT_TYPE }}.json types/${{ github.job.outputs.FAILED_TO_LINT_TYPE }}
      - name: Upload failed type
        uses: actions/upload-artifact@v3
        if: failure()
        with:
          name: failed-types
          path: types

      # upload types on success
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          deploy_key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
          publish_branch: types
          publish_dir: ./types
          allow_empty_commit: false
